#include<stdio.h>
int main()
{
    // int i=34;
    // int *ptr=&i;
    // printf("the address of i is %u\n",ptr);
    // ptr++;
    // ptr++;
    // ptr++;
    // printf("the address of i is %u",ptr);
    // return 0;
    char c=34;
    char *ptr=&c;
    printf("the address of i is %u\n",ptr);
    ptr++;
    
    printf("the address of i is %u",ptr);
    return 0;
    
}