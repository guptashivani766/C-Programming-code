#include <stdio.h>
int main()
{
    //int a;b=a; declaration error shows
    //int v=3^3; //this is valid but only not print the v
    //Q.3 wap to check wheather a no. is divisible by 97 or not
    //   int num;
    //   printf(" enter the number \n");
    //   scanf("%d",&num);
    //   printf("divisibility test returns : %d\n",num%97);

    //Q.4 evaluate the expression
    int x = 2, y = 3, z = 3, k = 1;
    printf(" value of 3*x/y-z+k is:%d", 3 * x / y - z + k);

    return 0;
}